global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'somaai-monitor'

scrape_configs:
  - job_name: 'somaai-app'
    static_configs:
      - targets: ['app:8000']
    metrics_path: '/metrics'
    scrape_interval: 10s
